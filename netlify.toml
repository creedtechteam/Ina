[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.12.0"
  NPM_FLAGS = "--force"

# SPA fallback for client-side routing and IPFS-friendly 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
